
function Game() {
  // setup state machine
  // state is 'connecting'
  // on connected, fire event loadGambler()
  // on gamblerLoaded, fire event loadTables()
  // on tablePicked, fire event playCraps()

  var socket = io();
}
